{
  "name": "library-management-system",
  "version": "1.0.0",
  "description": "Library Management System - Web Development Lab Project",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start"
  },
  "keywords": ["library", "management", "react", "express", "rest-api"],
  "author": "Student",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
